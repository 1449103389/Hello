//对于条形图
1.	首先给xAxis的categories需要传一个字符串数组，作为x轴每一项的名字

2.	yAxis不需要传数据

3.	条形图中的数据需要通过series属性传递，并且也是传一个数组，数组每项的格式都时形如{name:'',data:[]}的json串，
	其中data属性的数组的大小不应大于x轴的项数
	
注：对于形如{a:x,b:y}的json串，可以通过 串名['项'] 的形式取出对应的值

//对于饼图
1.	通过series属性传数据时，格式为 [name:'',data[{name:'',y: },{name:'',y: }]],其中所有y的值加起来不用必需等于100，
	因为百分比是根据所有y值计算的
